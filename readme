./matchedPAtterns/k8sLocations.txt - локейшены кубернетес
./matchedPAtterns/Locations.txt - остальные локейшены
UnmatchedLocations.txt - локейшены для которых не был найден паттерн
duplicate_UnmatchedLocations.txt - список локейшенов которые при парсинге встречаются более одного раза
LocationsList.txt - список найденных локейшенов
patterns.txt - паттерны для поиска
upstreams.txt - апстримы по которым проходит первый поиск для 
NewParams.txt - параметры которые добавляются/удаляются из локейшенов
	__add - добавляет параметр если такового нет
	__replace - заменяет значение параметра на новое
	__delete - удаляет параметр
	разбиение строки заголовка
		от начала строки до ^^ - параметр
		от ^^ до << - значение параметра
		от << до __ - новое значение параметра в случае replace
		Даже если значения пустые, ^^,<< должны присутствовать.
NewParamsSliceByLocations.txt - Разрез NewParams.txt по локейшенам для удобства просмотра что куда добавлено/удалено.
strict в названии паттерна - 
	кол-во параметров должно совпадать в локейшене и в паттерне, параметры считаются равными если параметр в паттерне является подстрокой параметра в локейшене.
	При отсутствии кол-во параметров в паттерне может быть меньше чем в локейшене, проверка на совпадение параметров проводится по тем же правилам как и для strict.
& в начале строки паттерна - 
	параметры локейшена и паттерна разбиваются на слова по символу пробела, после чего происходит точное сравнение patternWords[i] == locationWords[i], i < patternWords.size()